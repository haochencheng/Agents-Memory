# Qdrant 配置（docker-compose 使用）
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
# QDRANT_API_KEY=your-secret-key   # 生产环境取消注释

# memory.py 向量搜索配置
QDRANT_HOST=localhost
QDRANT_COLLECTION=agents-memory

# OpenAI（用于生成 embedding，只在需要 embed/vsearch 时设置）
# OPENAI_API_KEY=sk-...
